name: Node.js CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Get code
        uses: actions/checkout@v4   # ✅ updated version

      - name: Install dependencies
        run: npm install

      - name: Check app (basic test)
        run: echo "Build successful"